<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.easypan.mappers.EmailCodeMapper">
<!--   实体类映射 -->
	<resultMap id="base_result_map" type="com.example.easypan.entity.po.EmailCode">
		<!--   邮箱 -->
		<result column="email" property="email" />
		<!--   验证码 -->
		<result column="code" property="code" />
		<!--   创建时间 -->
		<result column="create_time" property="createTime" />
		<!--   0未使用1已使用 -->
		<result column="status" property="status" />
	</resultMap>
	<!--   通用查询列 -->
	<sql id="base_column_list">
		email,code,create_time,status
	</sql>
	<!--   通用基础查询条件 -->
	<sql id="base_query_condition">
		<if test="query.email != null and query.email != ''  ">
			and email = #{query.email}
		</if>
		<if test="query.code != null and query.code != ''  ">
			and code = #{query.code}
		</if>
		<if test="query.createTime != null ">
			and create_time = #{query.createTime}
		</if>
		<if test="query.status != null ">
			and status = #{query.status}
		</if>
	</sql>
	<!--   通用扩展查询条件 -->
	<sql id="base_query_condition_extend">
			<if test= "query.emailFuzzy != null and query.emailFuzzy!= '' ">
				and email like concat('%', #{query.emailFuzzy},'%')
			</if>
			<if test= "query.codeFuzzy != null and query.codeFuzzy!= '' ">
				and code like concat('%', #{query.codeFuzzy},'%')
			</if>
			<if test= "query.createTimeStart != null and query.createTimeStart!= '' ">
				 <![CDATA[ and create_time >= str_to_date(#{query.createTimeStart}, '%Y-%m-%d' ) ]]>
			</if>
			<if test= "query.createTimeEnd != null and query.createTimeEnd!= '' ">
				 <![CDATA[ and create_time < date_sub( str_to_date(#{query.createTimeEnd}, '%Y-%m-%d' ),interval -1 day) ]]>
			</if>
	</sql>
	<!--   扩展查询条件汇总 -->
	<sql id="query_condition">
		<where>
			<include refid="base_query_condition"/>
			<include refid="base_query_condition_extend"/>
		</where>
	</sql>
<!--   查询集合 -->
	<select id="selectList" resultMap="base_result_map">
		SELECT <include refid="base_column_list" /> FROM email_code <include refid="query_condition"/>
		<if test="query.orderBy != null"> order by ${query.orderBy} </if>

		<if test="query.simplePage != null"> limit #{query.simplePage.start},#{query.simplePage.end} </if>

	</select>
<!--   查询数量 -->
	<select id="selectCount" resultType="java.lang.Integer">
		select count(1) from email_code
		<include refid="query_condition"/>
	</select>
<!--   插入数据 -->
	 <insert id="insert" parameterType="com.example.easypan.entity.po.EmailCode">
		INSERT INTO  email_code
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.email != null"> 
				email,
			</if>
			<if test="bean.code != null"> 
				code,
			</if>
			<if test="bean.createTime != null"> 
				create_time,
			</if>
			<if test="bean.status != null"> 
				status,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.email != null"> 
				#{bean.email},
			</if>
			<if test="bean.code != null"> 
				#{bean.code},
			</if>
			<if test="bean.createTime != null"> 
				#{bean.createTime},
			</if>
			<if test="bean.status != null"> 
				#{bean.status},
			</if>
		</trim>
	</insert>
<!--   插入或者更新数据 -->
	 <insert id="insertOrUpdate" parameterType="com.example.easypan.entity.po.EmailCode">
		INSERT INTO  email_code
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.email != null"> 
				email,
			</if>
			<if test="bean.code != null"> 
				code,
			</if>
			<if test="bean.createTime != null"> 
				create_time,
			</if>
			<if test="bean.status != null"> 
				status,
			</if>
		</trim>
		<trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.email != null"> 
				#{bean.email},
			</if>
			<if test="bean.code != null"> 
				#{bean.code},
			</if>
			<if test="bean.createTime != null"> 
				#{bean.createTime},
			</if>
			<if test="bean.status != null"> 
				#{bean.status},
			</if>
		</trim>
		on DUPLICATE key update
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="bean.createTime != null"> 
				create_time= VALUES(create_time),
			</if>
			<if test="bean.status != null"> 
				create_time= VALUES(create_time),status= VALUES(status)
			</if>
		</trim>
	</insert>
<!--   批量插入数据 -->
	<insert id="insertBatch" parameterType="com.example.easypan.entity.po.EmailCode" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO  email_code(email,code,create_time,status) values
		<foreach collection="list" item="item" separator="," >
		(
		#{item.email},#{item.code},#{item.createTime},#{item.status}
		)
		</foreach>
	</insert>
<!--   批量插入或者更新数据 -->
	 <insert id="insertOrUpdateBatch" parameterType="com.example.easypan.entity.po.EmailCode">
		INSERT INTO  email_code(email,code,createTime,status) values
		<foreach collection="list" item="item" separator="," >
		(
		#{item.email},#{item.code},#{item.createTime},#{item.status}
		)
		</foreach>
		on DUPLICATE key update
		create_time= VALUES(create_time),
		status= VALUES(status)
	</insert>
<!-- 根据EmailAndCode查询  -->
	<select id="selectByEmailAndCode"  resultMap="base_result_map">
	select <include refid="base_column_list"/>  from  email_code where email=#{email} and code=#{code}
	</select>

<!-- 根据EmailAndCode更新-->
	<update id= "updateByEmailAndCode" parameterType="com.example.easypan.entity.po.EmailCode">
		UPDATE  email_code
		<set>
			<if test="bean.email != null" >
				email=#{bean.email},
			 </if>
			<if test="bean.code != null" >
				code=#{bean.code},
			 </if>
			<if test="bean.createTime != null" >
				create_time=#{bean.createTime},
			 </if>
			<if test="bean.status != null" >
				status=#{bean.status}
			 </if>
		</set>
		where email=#{email} and code=#{code}
	</update>

<!-- 根据EmailAndCode删除-->
	<delete id= "deleteByEmailAndCode" parameterType="java.lang.Integer">
		delete from email_code where email=#{email} and code=#{code}
	</delete>

	<!-- 根据EmailAndCode删除-->
	<update id= "disableEmailCode" parameterType="com.example.easypan.entity.po.EmailCode">
		update email_code set status=1  where email=#{email} and status=0
	</update>
</mapper>
